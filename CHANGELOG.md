# Changelog

All notable changes to Agent2SSH are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/).

## [Unreleased]

### Added
- **Opt-in sshd fixture tests**: `src-tauri/tests/exec_fixture.rs` runs against a real sshd and covers the runtime behaviours the unit suites structurally cannot reach — draining stdout and stderr off one SSH channel, bounding a remote command by its timeout, and collecting a health snapshot, which shares that drain. The suite is skipped unless `AGENT2SSH_TEST_SSH_PORT` points at a reachable server; `scripts/sshd-fixture/Dockerfile` builds a throwaway alpine/OpenSSH container to point it at. The bugs below all passed the full unit, CLI and daemon suites before these tests existed.
- **Tauri feature coverage in CI**: A new `tauri-unit-tests` job compiles, lints and tests the default feature set, so `src/tauri_commands.rs` — behind `#[cfg(feature = "tauri")]` — can now fail a pull request. Every other cargo invocation in CI passes `--no-default-features`, which left that module uncompiled, untested and unlinted.
- **Exec fixture suite is no longer local-only**: It now also runs from `scripts/e2e-docker.sh`, and therefore from the `real-ssh-e2e` CI job, against the container that script already starts. It accepts `AGENT2SSH_TEST_SSH_KEY` for that key-only server.
- **Empty-target and zero-budget unit tests**: `exec_multi_core`, `exec_multi_with_strategy` and `preview_exec_multi` have tests pinning the `no hosts matched the requested hosts or tags` error, so the preview cannot drift back to reporting an empty plan as success. `BoundedCapture` gained boundary tests for a zero budget (keeps no bytes) and a one-byte budget (keeps the newest byte, since the odd byte is all tail).
- **Remote rename and delete on every surface**: `sftp_rename_core`, `sftp_remove_file_core`, `sftp_remove_dir_core` and `sftp_remove_dir_all_core` (the last a breadth-first recursive delete) were implemented and covered by `core.rs` tests, and `store.rs` already reserved classifier branches for the `sftp_rename` / `sftp_remove` action labels — but no CLI subcommand, MCP tool, daemon route, Tauri command or desktop control called them, so they were unreachable from every entry point. They are now wired end to end: `agent2ssh sftp rename|rm|rmdir|rm-rf`; the MCP tools `ssh_sftp_rename` / `ssh_sftp_rm` / `ssh_sftp_rmdir` / `ssh_sftp_rm_rf` (`tools/list` grows from 54 to 58 entries); `POST /sftp/rename|rm|rmdir|rm-rf` on the daemon; and the desktop commands `sftp_rename` / `sftp_remove_file` / `sftp_remove_dir` / `sftp_remove_dir_all`, surfaced as a right-click menu on remote rows in the SFTP panel. Every path reuses the existing operation gate, so the approval policy, risk override and audit behaviour are the same ones `sftp mkdir` already had.
- **`ssh-keygen -R` equivalent for the system OpenSSH `known_hosts`**: G13 shipped a one-way import — `import_known_hosts_from_ssh` parses OpenSSH's `~/.ssh/known_hosts` into the app store, exposed as `import_known_hosts`, `agent2ssh known-hosts import` and `api.importKnownHosts`. Its design note then concluded that the reverse direction was impossible, because a trusted-host record keeps only a SHA256 fingerprint and no public key. That reasoning holds for *writing* new entries but says nothing about *deleting* existing ones, so `remove_system_known_host` — the `ssh-keygen -R` equivalent — sat in `embedded_ssh.rs` with no caller anywhere in the repository. It now reaches both surfaces the import half already had: the desktop command `forget_system_known_host` backs a per-host "Forget in OpenSSH known_hosts" action in Host Management, and `agent2ssh known-hosts forget <host> [--port N] [--json]` completes the CLI, whose `KnownHostsCommands` enum had carried a lone `Import` variant since G13. `forget` matches the bare-hostname and `[host]:port` forms, rewrites the file in place leaving a `.bak`, leaves hashed entries and unrelated aliases alone, and prints a notice rather than failing when nothing matched — the same contract `ssh-keygen -R` has. Export (writing entries) remains unimplemented and, as the note says, unimplementable from a fingerprint-only store.
- **Editable SSH algorithm preferences**: `ssh_algo::save_algo_prefs` wrote `ssh_algos.json` and `embedded_ssh` read it before every handshake, but nothing in the application could write the file — it was a hand-edit-only setting documented nowhere, so the reader and the writer disagreed about whether the feature existed. Settings now has an "SSH algorithms" section whose dialog edits all eight lists (key exchange, host keys, ciphers, MACs and compression, each direction), shows which values diverge from the built-in defaults, and reverts in one click. Changes take effect on the next connection, because `load_algo_prefs()` is read per handshake; already-open sessions keep the algorithms they negotiated.
- **Editable redaction rules**: The A24 lifecycle in `redaction.rs` — seed `redact_rules.json` on first run, load it thereafter, reset on demand — was written, covered by five passing tests, and called by nobody, because the live redaction path (`store::redact_sensitive_text`, and behind it every audit record, webhook notification, playbook result and diagnostic export) called `redact_default` unconditionally. Editing the file changed nothing: the green tests showed the functions worked, not that the feature existed. That path now reads the file, so `redact_rules.json` is the source of truth for every redaction the app performs. It is seeded from the built-in set on first run and falls back to it whenever the file is missing or unreadable, so a broken config can never make redaction *weaker*, and it is authoritative rather than additive — the same contract `copy_redact::redact_for_clipboard` already had. Writes go through `store::write_private_json`, so the file lands at `0600` like the rest of the config directory; the original code used a bare `fs::write`, which measures `0644` under the default umask (latent, since nothing reached those functions), and its `seed` created the parent directory while its `reset` did not. Settings gains a "Redaction rules" section that lists each pattern and its replacement and edits them in place — add, update and delete, plus a confirmed "Restore defaults"; see Changed for the guardrails that replaced an earlier read-only revision. CLI, MCP and daemon exposure is not part of this change. `load_user_rules` is deliberately uncached: it read and recompiled per call before, as `default_rules()` already did on the same path, and an mtime-keyed cache would have to cope with filesystem mtime granularity, where writing a file and reading it back in the same tick is not guaranteed to invalidate.
- **The three configuration lifecycles on the CLI, MCP and daemon**: `redaction.rs`, `highlight.rs` and `ssh_algo.rs` each carry a complete lifecycle — seed, load, add, update, delete, reset — and each was reachable from the desktop Settings panel and from nowhere else. They now reach all three automated surfaces, 13 operations apiece: `agent2ssh redact | highlight | algo`, the MCP tools `ssh_redact_rule_*`, `ssh_highlight_rule_*` and `ssh_algo_prefs_*` (`tools/list` grows from 58 to 71 entries), and the daemon's `GET|POST /redact/rules`, `POST /redact/rules/{update,delete,reset}`, the same four under `/highlight/rules`, and `GET|PUT|DELETE /algo/prefs`. Four decisions shaped the shape of it. First, `--daemon` is a *global* CLI flag, so a subcommand that ignored it would read the local file and print a confident wrong answer for `agent2ssh --daemon prod redact list` — worse than not supporting it — and all 13 commands route through one `remote_config` helper that resolves the alias, attaches the bearer token and decodes the response, rather than 13 near-identical functions each of which could get the auth header wrong. Second, a rule's pattern or keyword *is* its identity, and a regular expression (`(?i)password`) is not usable as a URL path segment, so identity travels in the request body and update and delete get their own POST paths instead of `/{pattern}`; this is the same reason `/approval/policies` is a body-carrying `PUT`. Third, removing a rule is confirmed on every surface — `--force`, `force: true` — because removing a redaction rule stops that class of secret being redacted in every audit record, webhook notification, playbook result and diagnostic export, while removing a highlight rule hides nothing the app writes but is still gated so a mistyped keyword cannot quietly drop a rule the user meant to keep. The check is repeated in all three layers rather than shared, because a client that speaks HTTP to the daemon never passes through the CLI or the MCP handler, and an MCP client that sends `"force": false` satisfies a `required` list that only proves the key is present. Fourth, `algo` is a singleton rather than a list — one `SshAlgoPrefs` of eight comma-delimited algorithm lists — so it is get/set/clear rather than five operations, and `set` starts from what is in effect and overrides only the lists the caller named, so tuning key exchange does not silently reset the other seven. `AlgoPrefsState` — the effective preferences, whether they are an override, and the defaults they fall back to — moves out of `tauri_commands.rs` into `ssh_algo.rs` so all four surfaces serve the same snapshot; it could not stay behind `#[cfg(feature = "tauri")]`, which the CLI, MCP and daemon do not compile with. `--literal` escapes the keyword at the call site rather than by handing `insert_rule` an `is_regex: false` rule, because that field is a migration input: `normalize_rule` consumes it by escaping the keyword and writing `true` back, so no non-regex rule is ever stored and `--literal` on a rule whose keyword is already escaped would escape it a second time and change what it matches. `highlight update --literal` therefore requires `--new-keyword`, and `highlight list` does not print a regex/literal column, because every stored rule is a regex and the column would say the same thing for all of them.
- **CLI contract tests run in CI**: `src-tauri/tests/cli_contract.rs` has covered the CLI's observable surface — argument parsing, exit codes, help text, version output and error messages — since B39, and no CI step ran it. The `build` job's cargo-test steps named `--lib`, `--test daemon_integration`, `--test cli_smoke` and `--test connect_deadline`, and `contract-consistency` ran nine name-filtered cases out of the first two of those suites. So the suite — 30 tests, 29 of them older than this change — could not fail a pull request, and the lifecycle contract test added by the previous change would have joined them as decoration. `build` gains a `Rust CLI contract tests` step alongside the `cli_smoke` one, so the whole suite now runs on all three platforms; it needs no fixture, because every test spawns the real binary against a temporary `AGENT2SSH_CONFIG_DIR`. `exec_fixture` deliberately stays out of the matrix: it needs the containerized sshd that `scripts/e2e-docker.sh` starts, which is why it runs from the `real-ssh-e2e` job and nowhere else.

### Changed
- **The settings panel uses the shared button primitives**: `SettingsMenu` was the last file in the app that hand-wrote every button it owned — 23 raw `<button>` elements built from two local class constants, with neither `<Button>` nor `<IconButton>` anywhere in it. It had been queued as a follow-up on the assumption that `Button` had no size for the panel's dense small buttons, and that premise did not survive contact: `IconButton` has carried the small square all along (`sm` is `size-7`), and 20 of the 23 sites are `h-9` full-width rows rather than small buttons. The premise that these were 23 different hand-rolled styles did not survive either — the two constants covered 20 of the sites, and one of them had grown a second shape as `cn(rowBtnCls, "justify-center px-2")` at eight more. What `Button` actually lacked was *shape*, not size: it is `inline-flex` and sized by its label, so it had no way to say "fill the container" (five call sites wrote `className="w-full"` by hand) and none to say "align the content left", which is what separates a settings row from a button. `Button` gains two additive axes, `block` and `align`; `defaultVariants` is untouched, so all 85 existing call sites are unchanged. The 23 sites become 22 `<Button>` and one `<IconButton>` — the close key, whose `X` is 15px, because `Button`'s base carries `[&_svg]:size-4` and `IconButton` deliberately does not resize the glyph it holds. Equivalence was measured rather than assumed: rendering each site's props through the real `cva` and diffing the class tokens against the string it replaced removes zero tokens in six of seven shapes, and the seventh only loses a `px-2.5` that `tailwind-merge` was already collapsing. That diff caught one real regression — `variant="outline"` brings `hover:bg-muted`, and a selected theme swatch had no hover classes before, so the primary tint would have been painted over on pointer-over and the selected theme would have read as unselected; the selected branch now restates its hover pair. Five changes are visible, all of them the price of adopting the shared spec: `cursor-pointer` on all 23 (they showed the default arrow), a `focus-visible` ring, glyphs at 14px instead of 16px — which is what every other `Button` in the app already renders, since `[&_svg]:size-4` is `calc(var(--spacing) * 4)` and this app sets `html { font-size: 14px }` — `px-4` instead of the UA's `1px 6px` on the seven full-width action buttons, and an explicit `text-foreground` where the panel's `text-popover-foreground` used to be inherited (those two tokens differ only in the `dark` and `nord` themes). `ui/button.test.tsx` pins both new axes and `ui/icon-button.test.tsx` pins why the close key is an `IconButton` at all; writing them first is what caught the axes being defined in the `cva` but never forwarded into `buttonVariants()`, which produced no classes and spread `block=""` onto the DOM while `tsc` and all 121 existing tests stayed green.
- **Redaction rules are editable, including deletion**: The first revision of this change shipped the panel read-only and exposed only "Restore defaults", reasoning that a rule set which can only gain rules cannot be a downgrade: restoring the built-in set re-enables a rule but never removes one, whereas deleting a rule stops a whole class of secret being redacted everywhere the app writes. The asymmetry is real, but it was the wrong thing to build the boundary on. The terminal highlight rules are edited in full thirty lines up in the same panel, so this restriction did not make the feature safer than its siblings — it only pushed add, update and delete back to hand-editing `redact_rules.json`, a file nothing in the app pointed at. The panel now offers all four operations, and the guardrail moved from withholding the operation to framing it: deleting opens a confirmation that names the consequence, built-in rules carry a badge so that confirmation can say a whole class of secret is about to stop being redacted, an empty replacement renders as `(removed)` rather than as a blank, and an empty rule set is shown as a warning instead of as a neutral empty list, because an empty `redact_rules.json` is a rule set that redacts nothing. `redaction.rs` gains `list_rules`, `insert_rule`, `update_rule` and `delete_rule` plus two error variants, `Duplicate` and `NotFound`: a pattern is a rule's identity, so a second rule for the same pattern, a rename onto an existing one, or an edit of a pattern that is no longer there are errors rather than silent overwrites, and every mutation validates the regex before writing so a bad pattern cannot leave a half-written set behind. `BUILTIN_RULES` is now one array that both `default_rules()` and the new `is_builtin_pattern()` read, so the seeded set and the marker the UI warns on cannot drift apart. The desktop commands grow from two to five, `RedactRuleInfo` (`pattern`, `replacement`, `is_builtin`) replaces the old wire shape, and the frontend's `RedactRuleConfig` type is deleted — nothing in the frontend reads the file, and after this change the mirror had no reference left.
- **WebDAV sync set convergence**: The desktop and CLI/daemon portable-config file lists were merged into the single `webdav_sync::SYNCABLE_FILES` constant, so every sync path now carries the same files. `webhook.toml` and `app_preferences.json` are synced from every entry point.
- **Local-only configuration files**: `secrets.enc`, `snippets.json`, and `approval_policies.toml` are no longer part of WebDAV sync and do not cross machines. Machines that relied on cloud sync for shared credentials or snippets must migrate them manually.
- **Byte units in the WebDAV Sync panel**: `formatBytes` now reports `KiB`/`MiB` consistently across the UI, replacing the panel's `KB`/`MB` labels.
- **`ExecRequest::timeout_secs` contract**: The deadline bounds how long the client waits, not the lifetime of the remote command. Documented the measured OpenSSH 9.7 behaviour (below) and directed callers that need a guaranteed stop to wrap the command server-side in `timeout(1)`.
- **`exec_multi` reports an empty target list**: `exec_multi_core`, `exec_multi_with_strategy` and `preview_exec_multi` resolve through one `resolve_exec_targets` helper that returns an error (`no hosts matched the requested hosts or tags`) for an empty resolved list, and propagate host/tag resolution failures instead of collapsing them to an empty vector. A `--tags` typo that selected nothing used to look like a successful run: the CLI printed an empty table and exited 0, MCP returned an empty array, the daemon returned 200, and `exec/preview` reported that a command nobody would receive was safe to send. The CLI, MCP server and Tauri command layer now surface it as a failure, and the daemon answers 400 for `/exec-multi` and `/exec/compare` — matching `/exec` and `/exec/preview`, which already used 400 for the same class of error rather than 500.
- **Connection and prompt budgets follow the caller**: `connect_tcp_address` tries every resolved address within one shared budget capped at 30 s, so a host whose unreachable AAAA record precedes a usable A record connects immediately instead of waiting out the first attempt, and a zero-second budget can no longer be passed to `TcpStream::connect_timeout` or `set_read_timeout` (both reject a zero duration). A failed connection now names every address that was tried instead of only the last one, so a host whose A and AAAA records fail for different reasons does not hide the relevant error, and addresses skipped once the budget ran out are reported as such. The keyboard-interactive external prompt wait now follows the caller's SSH timeout instead of a hard-coded 120 s.
- **Shared health-probe client**: `check_health_blocking` reuses one `reqwest::blocking::Client` instead of constructing one per daemon. Each client owns a dedicated runtime thread, so listing N daemons spawned and joined N of them per call.
- **One command-capture implementation**: `BoundedCapture`, the drain loop, `ExecTimeout` and the drain/teardown constants moved from `core` into `embedded_ssh` as `run_command_capture`, which both the exec path and the health collector now call. `core::EmbeddedExecOutput` was field-for-field identical to the new `CapturedExec`, so it is gone.
- **One TCP↔channel bridge**: `bridge_tcp_and_channel` now has a single implementation in `embedded_ssh`. The jump-host `direct-tcpip` bridge and the local/remote port forwarders all call it, with cancellation and the forwarder's live byte counters injected instead of duplicated, so a fix to the read or EOF handling reaches every tunnel rather than one of two copies. The `write_all_tcp`/`write_all_channel` wrappers the second copy needed are private now.
- **Stoppable forward writes**: `write_all_tcp` and `write_all_channel` gained `_with_stop` variants, and the port-forward bridge checks the stop flag between reads and inside each write loop. Removing a forward now interrupts an in-flight write instead of leaving the connection thread blocked until the peer drains, and the remote-forward target connection is opened through `connect_tcp_address` with a 5 s budget rather than an unbounded `TcpStream::connect`.
- **One name for the config-directory override**: `store::CONFIG_DIR_ENV` replaces 98 inline `"AGENT2SSH_CONFIG_DIR"` literals across the library and the four integration-test suites. The variable that points the CLI, daemon, MCP server and desktop at an isolated config directory is now declared once, next to the `store::config_dir()` that reads it, instead of being spelled out at every call site.
- **CI actions moved off the deprecated Node 20 runtime**: `actions/checkout` v4 → v7, `actions/setup-node` v4 → v7, `actions/cache` v4 → v6 and `softprops/action-gh-release` v2 → v3. Every run was emitting a Node 20 deprecation annotation because those releases target a runtime GitHub has started forcing onto Node 24. None of the intervening breaking changes apply here: checkout's fork-PR block only covers `pull_request_target` and `workflow_run` triggers (this workflow uses `push`/`pull_request`/`release`), and the rest are ESM migrations plus a runtime bump. `dtolnay/rust-toolchain` is a composite action with no Node runtime and stays as-is.
- **G13's "one-way import only" conclusion is narrowed**: `docs/rssh-design-analysis.md` recorded that exporting back to the system `known_hosts` was impossible — a trusted-host record keeps only a SHA256 fingerprint and no public key — and concluded the feature would therefore stay import-only. Only the export half is impossible: deletion matches on hostname, never on key material. The entry now says which direction is blocked and which is not, and its status line reads import plus forget rather than "known_hosts 双向导入导出".
- **One session input splitter**: The function that turns a session's pending buffer plus newly written bytes into whole commands plus a trailing fragment existed twice, byte for byte — in `tauri_commands.rs` for the desktop write path and in `bin/agent2ssh-daemon.rs` for the websocket handler. That splitter is what makes a fragmented write safe to authorize: a write can end mid-line, so a caller that authorized only the bytes it just received would let a command be assembled across several writes and run without ever being approved. Both surfaces therefore had to agree on how a line closes, and an edit to one copy that missed the other would have been a quiet authorization hole. It is now `session::split_completed_session_commands`, and the daemon needed no import change because it already glob-imports `agent2ssh::session`.
- **One multi-host authorization loop**: The desktop, CLI and MCP exec paths each carried their own copy of the same thirty-line loop — expand the host/tag selection, authorize each target individually, and collect the high-risk hosts that came back approved so `force` can be flipped for exactly those. That loop is where the multi-host authorization rule lives, so three copies meant three places that had to agree on it. It is now `execution_control::authorize_targets_without_approval_handler`, and each surface keeps only what is genuinely its own: the rejection wording, which is forwarded verbatim, and the rendering of the returned error (`String` for desktop, `anyhow` for the CLI, `McpError` for MCP). The expansion failure used to be mapped per-surface and is now folded into `CommandAuthorizationError::Internal` with its original message; all three surfaces already render `Internal` as the bare message, and `McpError::from(anyhow::Error)` is also `McpError::internal` with code `-32000`, so the text and code a caller sees are unchanged.
- **Shared proxy field trimming**: `core::normalize_proxy` and `store::normalize_config` each carried the same fourteen lines of proxy cleanup. Only the cleanup is common, so only the cleanup moved — `ProxyProfile::trim_fields` is a pure in-place trim. The failure policy deliberately stays per-surface: `normalize_proxy` validates a single proxy the user just submitted and names the first missing field, because that string goes back to the user, while `normalize_config` repairs a loaded `hosts.json` and silently drops entries that fail the same required-field checks, because one bad entry should not cost the whole file.
- **Audit-entry tests build on a fixture**: Fifteen test literals spelled out all thirteen fields of `AuditEntry`, mostly just to say "fresh id, fresh timestamp, every optional field empty". Each of the last four fields the struct gained — `side_effect`, `source`, `action`, `outcome` — had to be added to every one of them. They now override only what they assert on and take the rest from `AuditEntry::test_fixture()`. Sites that need a specific timestamp still pass one (anomaly offsets from now; the 5000-entry store test derives from a fixed epoch), because an explicit field beats the struct-update spread.
- **A test config directory that cleans up after itself**: Six modules each rebuilt the same scratch config directory — `temp_dir().join(..)`, `create_dir_all`, `store::set_test_config_dir`, and a cleanup call at the end of the test that every early `assert!` skipped. `store::TestConfigDir` now does all three in one guard and removes the directory when it drops. Two of the six were also wrong: `highlight`'s teardown only cleared the thread-local override and leaked the directory on every run, and the three `forward.rs` tests derived their path from `std::process::id()`, so tests in the same process shared one directory instead of getting their own.
- **One spinner mark**: `<Loader2 className="animate-spin" />` was written by hand in twelve places across six components, at four different sizes, so the mark was decided in twelve places. `ui/state.tsx` now exports `Spinner` — the icon is `aria-hidden`, so a decoration is no longer an unnamed graphic to assistive tech — and every one of those call sites plus `LoadingState` goes through it.
- **The spinner is now the only thing in the app that turns**: The `Spinner` entry above collected twelve hand-written `<Loader2 className="animate-spin" />` call sites, but a second way to draw a busy state had grown beside it and, counting outcomes, matched the first: eleven sites spun a button's *own* glyph behind a hand-written `className={busy ? "animate-spin" : ""}`, while eleven different sites swapped the button's glyph for `Spinner`. Only one of those two answers went through a component — with an `aria-hidden`, a default size and tests — and the other was re-decided across seven distinct busy expressions in six files (`refreshBusy`, `healthBusy`, `updateBusy === "check"`, `daemonActionBusy === "restart"`, `diagnosticBusy === "refresh"`, and more). All eleven now swap in `Spinner`. This direction was chosen over adding a `BusyIcon` because it needs no new abstraction and leaves one thing that turns instead of two named ones; the cost is that a button's glyph becomes a loading mark while its action is in flight, which is also the one visible change. `SettingsMenu` keeps its own 16px marks and `RecordingsPanel` keeps its inline label, since neither was part of the duplication. Two `IconButton` refresh marks move 14 → 15 to match the five static ones beside them — both sizes genuinely rendered, so a click used to shrink the glyph by a pixel as it started turning. Four `size` props are dropped rather than migrated: the shared `Button` base carries `[&_svg]:size-4`, which targets the `svg` element itself and so outranks the SVG `width` presentation attribute, meaning `size` inside a `Button` never applied and removing it changes nothing. The `progress` toast variant is folded in as well, since it spelled "spin" by hand a third time; it has no call sites today and its rendered class set is identical, so only the claim in `Spinner`'s own doc comment changed — it is true now.
- **One host picker**: `HostSelector` existed and three panels used it, but four more hand-rolled the same single-select over the `hosts` array, and they had drifted in four ways at once — three option texts (`name`, `name (host)`, `name - user@host:port`), two framings (bare in a toolbar, plain text under a heading), and a disagreement about the one thing that matters: whether an empty value is an answer. That is not a style difference. `ForwardPanel`'s jump host and `AddHostForm`'s bastion are optional, so `""` has to survive as a real answer, while the settings panels must never leave a stale host name in the field and so force a real host. `HostSelector` already knew how to force one; it had no way to express "none is fine", which is the whole reason the other four sites could not use it. The distinction is now a prop — `allowEmpty` says `""` is legal, so the control stops auto-selecting and stays usable with an empty host list — and `emptyOption` separates a blank entry the user may choose (`None`, `Direct connection`) from the placeholder shown when there is nothing to choose at all, which is not an answer. `optionLabel` lets each caller keep its own wording, which is what makes this a zero-visible-change merge, and `shell` plus `selectClassName` cover the two framings. Three sites that look the same are deliberately not: `AddHostForm`'s proxy select reads `proxies` rather than `HostProfile`, `MultiExecPanel`'s host control is a multi-select chip group, and `HostDiffView`'s picker selects a run result rather than a configuration. One deliberate difference remains: when the host list is emptied, a value that no longer names anything is cleared instead of left dangling — the two toolbar sites used to keep it and lean on the submit button's `disabled`, and clearing matches what this component already did for its three existing callers. All five call sites had no test coverage at all; there are now fifteen, asserting what `onChange` receives, whether the select is disabled, and each option's value and text rather than incidental spacing.
- **One inline alert box**: Thirteen call sites hand-rolled the tinted warning/error box and had drifted apart: three paddings (`px-2.5 py-2`, `px-3 py-2`, `p-3`), two border alphas (`/30`, `/40`), two font sizes (`text-xs`, `text-sm`) and two radii. The warning note shown inside destructive confirmation dialogs was byte-identical in six of them (`HostList` ×3, `ConfigSnapshotsPanel` ×2, `ProxyPanel` ×1). `ui/state.tsx` now exports `InlineAlert` with `warning` / `destructive` tones and an optional bordered-plus-icon form, and all thirteen sites go through it — a counterpart to the existing `ErrorState`, which stays the full-panel placeholder. Two visible deltas, both toward the shared spec: the algorithms dialog's error box grows from `text-xs` to `text-sm` with `px-2.5` → `px-3`, and the sync panel's warning note loses its one-off `rounded-lg`/`p-3`. `SetupWizard`'s local `errorCls` constant is gone. The SFTP transfer progress row was deliberately left alone: it is the same tint but a progress indicator (spinner leading, `items-center`, inline text), not an alert.
- **One private JSON write path**: `highlight.rs`, `copy_redact.rs` and `redaction.rs` each spelled out their own create-dir, serialize and write for their rule file, and only `copy_redact` called `restrict_file_to_owner`. `store::write_private_json` is now the single path for the small independent JSON files — create the parent, serialize pretty, write, harden — and both live writers go through it: `highlight::save_rules` and `copy_redact::save_copy_redact_rules`. The hardening sits inside the function so that the next rule file cannot miss it. It deliberately takes no lock and keeps no backup, unlike `save_config`, which owns both concerns for `hosts.json`. `highlight::save_rules` also stops discarding a `create_dir_all` failure.
- **One confirmation dialog**: Seven more sites rebuilt a card by hand — `HostList` (remove a host, remove the selection, delete a group), `ConfigSnapshotsPanel` (apply a template, restore a snapshot, delete one) and `ProxyPanel` (delete a proxy) — each a `max-w-sm` `Dialog` with a title paragraph, an alert and an `mt-4 flex justify-end gap-2.5` footer, all sitting next to a `ConfirmDialog` that could not express what they needed. It gained `note` for the consequence (six of the seven carried an `<InlineAlert>` there; the seventh carried nothing) and `confirmDisabled` for the `disabled={busy}` the three snapshot dialogs need while an action is in flight. Its buttons also drop `size="sm"`: every other confirm footer in the app is the default size, `ApprovalDialog` included, and with a 14px base `sm` put a modal's labels below its own body text. Two visible deltas, both landing on the seven callers that already used `ConfirmDialog` — their buttons grow to the default size, and their six Cancel buttons become translated (see Fixed). The seven that migrated in keep their button size and gain a `font-medium` title, plus an explicit `text-foreground` where they previously inherited the surface's `popover-foreground`: the two tokens are the same colour in four of the six themes and differ in the dark and nord ones, where those titles read slightly dimmer than before.

- **One write path for every rule file, including `ssh_algos.json`**: `store::write_private_json` exists so that "create the directory, serialize, write, restrict to the owner" cannot be spelled out slightly differently in each of the files that needed it — its doc comment already listed `highlight_rules.json`, `copy_redact_rules.json` and `redact_rules.json` as callers, and `ssh_algo.rs` was a fourth that still inlined all four steps. Converging it removes the redundant `ensure_config_dir()` call and one more place the hardening step could be dropped; the doc comment now names the file. No behavioural change: the permissions it was setting and the ones `write_private_json` sets are the same `0600` (`icacls` on Windows).
- **Every daemon route is documented, and a test keeps it that way**: `docs/api.yaml` is the published contract, and seven routes had drifted out of it — `/snippets`, `/snippets/{name}`, `/forwards/multi`, `/forwards/{id}/start`, `/forwards/{id}/stop`, `/prompts/pending` and `/prompts/respond`. Each gap is invisible from either side alone: the route works, the server serves it, and no client can discover it; and the two halves live in different files that nothing compared. The 13 new routes are documented alongside them, and `api_yaml_documents_every_daemon_route` now fails in both directions — a route with no spec entry, and a spec entry with no route — so a change that adds one has to add the other. The only two exempted paths are `/` and `/console`, which serve the web console rather than the API. `CLAUDE.md` pointed at `docs/api.yaml` with a "keep in sync" note that nothing enforced; it now names the test.
- **`docs/skills.md` is mechanically tied to the tool registry**: two of the three parity checks already existed — `mcp_tool_list_contains_exactly_71_tools` counts the registry and `mcp_tools_match_skills_md_documentation` compares names in both directions — but the tool count had also been spelled out in prose across `README.md`, `docs/README.md`, `docs/architecture.md`, `docs/guides/{help,mcp-client-templates,mcp-quickstart}.md` and `docs/skill-distribution.md`, fifteen occurrences of one number that no test could see. They are updated here; the reason they can drift is that they are prose, and the two tests above remain the only thing that can notice.
- **The embedded skill documents the configuration lifecycles**: `skills/agent2ssh/SKILL.md` is the convention sheet an agent reads before calling any of this, and it described `force: true` for high-risk commands but said nothing about the three lifecycles that live under `~/.agent2ssh/`. It gains a `Local Configuration` section covering the four things a caller gets wrong — that removal needs confirmation, that a pattern or keyword is an identity so duplicates are refused rather than merged, that only `redact` validates its pattern, and that `algo set` touches only the lists named. The version moves `0.2.1` → `0.3.0` so an existing installation is offered the update; `integrate.rs` compares the installed and embedded versions for equality, so a content change without a version bump would never be installed.

### Removed

- **Intel macOS support**: `x86_64-apple-darwin` is no longer built, tested or published. The `build` and `tauri-bundle` matrices drop that leg, so releases no longer carry `agent2ssh-*-x86_64-apple-darwin.tar.gz` and no DMG is produced for Intel Macs. The Homebrew formula's Intel branch now fails with an explicit message rather than fetching an archive that no longer exists, and the README plus the MCP distribution and branch-protection guides list the supported platforms as macOS (Apple Silicon), Linux x86_64 and Windows x86_64. Intel Macs can still build from source; that architecture is simply no longer tested.
- **Four helpers that only forwarded or duplicated a live path**: `AppState::emit_bus`, `Host::is_headless`, `Host::is_cli` and `policy::policy_risk_rules` had no caller anywhere in the repository. `emit_bus` only forwarded to `events::publish_event`, which has 15+ direct callers, so routing emissions through `AppState` would have added a layer without changing behaviour. `is_headless` / `is_cli` restated `transport_name()`, which is the accessor actually used by `diagnostics` and by the audit `source` default. `policy_risk_rules` was the interesting one — it is not an unwired feature but a *worse* copy of `risk_config::load_risk_rules()`: it reads only `policy.toml`'s `risk` table and ignores the standalone `risk_rules.toml` fallback and the mtime cache `load_risk_rules()` provides, so wiring it up would have silently dropped every rule that file declares. All four are gone, along with the `TODO(unwired)` notes that had outlived them. `Host::emit` is **kept**: it is covered by tests and is the migration target the `Host` enum documents for `app.emit()`.
- **The test-only glob matcher in `ssh_config.rs`**: Include expansion goes through the `glob` crate — `splice_pattern` checks `has_glob_meta` and hands the pattern to `glob::glob` — but the module also carried a hand-rolled `glob_match` / `glob_match_bytes` / `char_class_match` (about 95 lines, all `#[cfg(test)]`) that no production path called. It existed only so six unit tests could exercise it, which meant those six were testing a copy of glob's semantics rather than anything this crate does. `include_splices_glob_matches` and `include_splices_multi_level_glob_matches` build real `config.d` trees in a temp dir and assert on what `load_with_includes` returns, so include expansion itself stays covered. `has_glob_meta` is production code and keeps its test.
- **The `rand` dependency**: `backup_crypto` was the only place in the crate that pulled in `rand`, and only for two `thread_rng().fill_bytes()` calls filling the Argon2 salt and the AES-GCM nonce. Every other entropy call site — `keys.rs`, `mcp_binding.rs`, `secrets.rs` — already used `getrandom::fill`, so dropping it leaves one RNG entry point instead of two that had to stay consistent. No behavioural change: `thread_rng` is itself seeded from the OS CSPRNG. `cargo tree -i rand` showed `agent2ssh-app` as the sole reverse dependency; `rand_core` / `rand_chacha` / `ppv-lite86` stay in the lock because other transitive crates still need them.

### Fixed
- **Redaction-rule failures were all reported as an invalid regex**: `RedactRuleError` carried only `InvalidRegex` and `ZeroWidth`, so the rules-file lifecycle had no way to say "the config directory is unavailable" or "this file is not JSON" and said `invalid regex pattern: cannot determine config directory` instead. It gains `IoError` and `ParseError`, a JSON parse failure is no longer labelled a regex failure, and the message Settings shows names what actually went wrong. Latent rather than live, since nothing could reach those functions before this release.
- **The desktop app reported itself as the CLI**: `Host::is_desktop()` is `matches!(self, Host::Tauri(..))` — it tests the *value*, not whether the `tauri` feature was compiled — but `run_tauri`'s startup comment claimed the opposite ("`is_desktop()` returns true under `#[cfg(feature = "tauri")]`") and the desktop therefore never installed a `Host`. `set_host` had exactly one caller in the whole repository: the daemon's `Host::Headless`. The desktop left the global at its `Host::Cli` default, so the `diagnostics` report showed `"transport": "cli"` and `"is_desktop": false` inside the desktop app, and every transport-aware branch would have taken the CLI path. `run_tauri` now calls `set_host(Host::Tauri(app.handle().clone()))` from its setup hook, the comment describes the real mechanism, and two unit tests pin the value-based semantics so a missed `set_host` cannot pass unnoticed again.
- **CI could not start after that action bump**: This repository restricts Actions to an explicit allow-list whose patterns are matched against `owner/repo@ref` — so they pin the version too — and `softprops/action-gh-release@v2` does not permit `@v3`. Job creation failed and the run reported `startup_failure` with zero jobs, no log and no re-run option. `github_owned_allowed` covers `actions/*` at any version, which is why only the third-party action was blocked. The allow-list now mirrors the versions the workflow pins, the constraint is documented in the workflow header, and `workflow_dispatch` was added so a run that dies during startup can be retried without a commit.
- **stdout/stderr drain deadlock**: `exec_ssh_embedded` drained stdout to EOF before reading stderr. The SSH channel window is shared between the two streams, so a command that filled stderr while still holding stdout open blocked its own writes and the read never returned. Both streams are now read on every pass, EOF is taken from `Channel::eof`, and the deadline is enforced inside the loop. `max_output_bytes` is applied per stream via a head-and-tail accumulator, so a stderr flood can no longer grow the process without bound.
- **Timed-out execs no longer leak a blocking worker**: The timeout path closed the channel while the session had already been switched back to blocking mode, and a blocking `close` waits for a server acknowledgement that only arrives once the remote command exits — so a 2 s deadline on an 8 s command returned after 8 s, and the abandoned `spawn_blocking` task kept the caller's runtime alive. The session now stays non-blocking through the teardown, and the post-drain exchange is bounded by an explicit session timeout instead of blocking indefinitely.
- **Timed-out execs are audited**: A timeout previously returned straight out of the exec and wrote no audit entry, even though the command did run. Timeouts are now recorded with no exit code and a reason of `command timed out after Ns`.
- **Bounded stderr capture**: `max_output_bytes` now caps stderr as well as stdout (each stream keeps its head and tail, and `dropped_bytes` reports the total discarded). Stderr was previously read to EOF with no limit, so a chatty remote command could grow the process without bound.
- **WebDAV failures are surfaced**: The desktop sync actions and the automatic post-change sync now read `lastSuccess` off the returned status. Failures previously showed a success toast, and automatic sync failures produced no toast and no diagnostic at all, because the backend reports sync errors as a status rather than as a rejected call.
- **Unix PATH detection**: `path_contains_dir` splits on the platform separator (`:` on Unix, `;` on Windows) instead of always `;`. Splitting on `;` made the whole Unix PATH a single segment, so the CLI status panel always reported the binaries as absent from `PATH`.
- **Ephemeral-port port forwards**: `bind_port = 0` now binds IPv6 loopback on the port IPv4 actually received. Binding both stacks on the literal `0` gave them two different ephemeral ports while only IPv4's was recorded, so clients resolving `localhost` to `::1` reached the wrong port.
- **The confirmation dialog's Cancel button was never translated**: `ConfirmDialog` defaulted `cancelLabel` and `confirmLabel` to the string literals `"Cancel"` and `"Confirm"` instead of running them through `t()`, and six of the seven `confirmDialog()` callers omitted `cancelLabel` — so each of those six rendered an English Cancel button in the Chinese UI, while `"Cancel"` sat translated in the zh map for the fourteen other call sites that spell it out. The defaults now resolve through `t()`, which makes the omission unreachable for future callers as well, and `"Confirm"` gained the zh entry it had never needed until the default could actually render.
- **`highlight_rules.json` was readable by any local account**: `highlight::save_rules` wrote the rule file and stopped there, so on Unix it landed at whatever the umask allowed — `0644` (`-rw-r--r--`) under the usual `022` — while about forty call sites across sixteen files hold every other file under `~/.agent2ssh/` at `0600`. `copy_redact_rules.json` did harden itself, and its comment records that an earlier module-local helper was Unix-only and silently did nothing on Windows; `highlight` never had the step on either platform. Both writers now go through `store::write_private_json`, which cannot be bypassed by a caller that forgets, and `highlight`'s regression test pins the `0600` mode.
- **Legacy WebDAV markers**: A pull from a marker that still lists a dropped or never-syncable file (`known_hosts.json`, `secrets.enc`, `snippets.json`, `approval_policies.toml`) is accepted and the entry is skipped, rather than failing the whole pull. This restores the 0.3.0 contract that a stale remote manifest cannot overwrite local SSH host-key trust state, credentials, or snippets.
- **Red clippy gate**: `execution_control::authorize_command_without_approval_handler` trips `clippy::too_many_arguments` (11 against a threshold of 7), so both `Rust clippy` steps in `contract-consistency` fail and the whole workflow is blocked — the commit that added it (`f262505`) ran `cargo check` but not clippy. The library also carried five needless borrows and a redundant match guard in the tray setup, invisible because no CI step enabled the tauri feature. All six are resolved.
- **Leaked workers on a silent peer**: `connect_embedded_ssh` set socket-level read/write timeouts but no libssh2 session timeout, and libssh2's blocking-mode waits loop internally without a deadline. A host that accepted the TCP connection and then said nothing — no SSH banner — left `handshake` blocked forever. `ping_hosts_core` and `collect_health_snapshot` wrap the connect in `tokio::time::timeout`, so they returned on schedule, but abandoning the `spawn_blocking` handle leaked the blocked worker and its socket, once per host and per poll (a host list containing one broken entry accumulated threads). The session now gets the same budget the socket has, so blocking calls fail instead of hanging; the same budget also bounds a mid-session stall on any other blocking transport call.
- **IPv6 literals in host:port strings**: `format_host_port` now brackets IPv6 literals, so `::1` formats as `[::1]:22`. The HTTP-proxy path built its authority from the raw host, so `CONNECT ::1:22 HTTP/1.1` was malformed and every IPv6 host routed through an HTTP proxy failed; the fingerprint/trust prompts, connection info and exec audit entries also printed an ambiguous `::1:22`. The known-hosts trust key is unchanged (it is built from the host identity, not from this string), so no re-trusting is required.
- **Daemon health probes ignored the scheme and the hostname**: `check_health_blocking` hand-rolled a plaintext `GET /health` over a raw TCP socket, so an `https://` remote was probed in the clear, and any `host:port` it could not parse silently fell back to the local daemon address — a malformed remote URL therefore reported itself as `connected: true`. The probe now uses the configured scheme and hostname through a real HTTP client under a single 2 s budget and accepts only a 2xx response. `agent2ssh daemon diagnose` likewise resolves DNS names instead of rejecting everything that is not an IP literal.
- **Health collection carried the exec drain deadlock**: `health::run_health_command` read stdout to EOF before it touched stderr and capped neither, so a remote whose login shell or profile filled stderr while stdout was still open blocked its own writes — the same shared-channel-window deadlock `exec_ssh_embedded` was fixed for. It also enforced no deadline of its own, relying entirely on the caller's outer `tokio::time::timeout`. It now runs through the shared capture path with the caller's timeout as its deadline and a 64 KiB bound per stream.
- **CI could not run the tauri feature on a bare runner**: The `tauri-unit-tests` job ran `cargo clippy --lib -- -D warnings` and `cargo test --lib`, and those compile `build.rs`, where `tauri_build::build()` requires every `bundle.externalBin` sidecar to already exist as `src-tauri/binaries/<name>-<host-triple>`. Those are gitignored build outputs that only `tauri-bundle` produces, so a Linux runner has none and the build script aborted with `resource path binaries/agent2ssh-cli-x86_64-unknown-linux-gnu doesn't exist` — the job failed before compiling a single test, which is why the gate had never actually run. It now blanks `externalBin` via `TAURI_CONFIG`, which `tauri-build` merges over `tauri.conf.json` (an empty list is a no-op, and the job neither packages nor launches the app). Reproduced by removing the host sidecars locally; both commands now pass with none present.
- **Remote `rm -rf /` was rated low risk and would have been auto-approved**: The SFTP surfaces never execute a shell — they synthesise `sftp <operation> <path>` strings purely to drive risk classification, the approval policy and the audit action. `classify_risk_single` only recognised shell verbs, so the canonical head was always `sftp`, none of the `rm` rules matched, and everything fell through to the default verdict. Measured before the fix: `sftp rm-rf /` → **Low**, `sftp rm-rf <path>` → Low, `sftp rm <path>` → Low, `sftp rename` → Low. Because approval is opt-in (no matching policy means auto-approve), wiring the new operations up without addressing this would have let a recursive delete of a remote `/` run silently. `classify_risk_single` now mirrors the shell table for `sftp`: `rm-rf` against `/`, `/*` or `/.` is **Blocked**, any other `rm-rf` is **High**, `rename` is **Medium**, and `rm` / `rmdir` stay **Low** to match `rm <path>` / `rmdir <path>`. Two tests pin the mapping, one of them asserting that a `;`-chained payload hidden in the path operand is still caught by chain splitting rather than by the new branch.
- **Algorithm preferences failed closed with nothing validating the file**: `apply_algo_prefs` returns an error for an algorithm name libssh2 does not know, and it runs immediately before every handshake — so a single typo in a hand-edited `ssh_algos.json` broke *every* connection, not one. Since the app now writes that file, `save_algo_prefs` validates before persisting: all eight fields non-empty, names limited to `[A-Za-z0-9._\-@+]`, no duplicates within a field, and both compression fields exactly `none` (the `ssh2` crate is built without zlib, so any other value is rejected here rather than at connect time). The rejection names the offending field. `clear_algo_prefs` deletes the file and falls back to the built-in defaults, and is idempotent when the file is already absent.
- **Stale MCP tool counts**: Growing `tools/list` from 54 to 58 entries left `54` behind in two test assertions — the registry-count meta-test in `daemon_integration.rs` and the stdio `tools/list` length check in `cli_smoke.rs` — so both suites failed while `docs/skills.md` (whose test compares tool names, not a total) stayed green and hid the omission. Both assertions now read 58, and the same stale count was corrected in 14 places across eight documentation files, including the `MCP Tools (54)` heading in the README and the `前 31 个` reference to the quickstart's partial table, which now lists 35 entries.
- **Wall-clock flake in the approval TTL tests**: `test_timed_out_approval_cannot_be_approved` failed roughly once in six full-suite runs and never in isolation. The TTL check compares wall-clock `Utc::now()` timestamps, while `tokio::time::sleep` advances the *monotonic* clock; a backward wall-clock step during a single `sleep(2s)` on a 1 s TTL leaves `elapsed` just under two seconds, `num_seconds()` truncates it to 1, and `1 > 1` is false — so the respond succeeded and the assertion failed. The four TTL tests now wait on a `wait_until_expired` helper that polls `approval_poll` until the expiry is actually observed under a 60 s deadline, which makes the wait provable instead of assumed and names the approval in the failure message if it never expires.
- **Two `TODO(unwired)` notes outlived the code they described**: `core.rs` still said the SFTP rename/remove cluster was "fully implemented but currently unreachable — no `#[tauri::command]` wrapper is registered for it ... and no CLI/MCP/daemon surface calls it either", and `embedded_ssh.rs` still said `remove_system_known_host` had "no caller in the repository". Both were accurate when written and false once the wiring above landed — the worst state for a note of that shape, because the obvious way to check what is still unwired is to grep for exactly this string, and it pointed at two things that are now live. Both are replaced with a description of where the code is actually called from. Grepping for them is also what surfaced the CLI gap above, so the habit was worth more than the cleanup.
- **`HighlightError` produced translation keys that were never translated**: its `Display` wrote `highlight_not_found` and `highlight_keyword_conflict` — shaped like i18n keys, and treated as such, but there is no entry for either in the i18n table and no consumer that resolves one: the desktop's `HighlightSettings.tsx` passes `String(err)` straight to `setMessage`, so those keys were rendered to the user verbatim. The variants now carry the keyword that failed and the `Display` impl writes a sentence, which is what the one existing consumer has always needed; the three tests that asserted on the variants use `matches!` with the payload rather than comparing the old key string.
- **Three contract descriptions claimed validation that does not happen**: writing them is what exposed it, because each was checked against the code rather than against the neighbouring prose. (1) The `ssh_algo_prefs_set` MCP description said names are "validated against the algorithms this build supports, and an unknown name is rejected rather than dropped" — `validate_algo_prefs` checks shape only: non-empty, no duplicates, and characters limited to letters, digits and `._-@`. A well-formed name libssh2 has never heard of is accepted here and fails later, at the handshake, which is precisely the outcome the sentence promised would not happen; it now describes the shape check. (2) `docs/api.yaml` listed "empty replacement" among the 400s for `POST /redact/rules` and `POST /redact/rules/update` — `RedactRule::new` validates the pattern and nothing else, and an empty replacement is a legitimate way to delete the matched text rather than substitute it. (3) The same file listed "invalid regex" among the 400s for `POST /highlight/rules` and `POST /highlight/rules/update` — `highlight::validate_rule` checks the keyword, the name and the colour, and an explicit comment says why it stops there: the regex is compiled by JavaScript in the xterm renderer, whose grammar differs from Rust's (no look-around, for one), so a Rust-side check would reject patterns that work and accept patterns that do not. The asymmetry between the two families is now stated where a caller will look for it rather than left to be discovered, and both halves are pinned by `redact_highlight_algo_help_and_local_crud_contract`.
- **`CLAUDE.md` had been calling it 51 tools**: two lines described the MCP server as exposing "51 tools" and `docs/skills.md` as the reference "for the 51 MCP tools", seven growth steps behind. Unlike `docs/reports/REGRESSION-LOG.md` and the `[0.1.0]` changelog entry — snapshots that record what was true when they were written and would be falsified by an edit — this is a live instruction file whose whole purpose is to tell an agent working in this repository where things are, so a stale count there is an active error source. Both lines now read 71, and both name the test that will fail if they drift again.
- **The comment on `reset_default_rules` still argued the old boundary**: it said add/remove/update are "deliberately not [safe to expose]", which was the judgement the Changed entry above replaced. It survived that revision because the revision rewrote the panel, the commands and the note explaining them, and this is the fifth place the same claim had been written — the other four were updated in the earlier pass, so a grep for the old wording would have found this one and a grep for the new would not. It now records what the original judgement got wrong and what replaced it, so the next reader inherits the reasoning rather than the conclusion.

### Verified
- `src-tauri/tests/connect_deadline.rs` drives `ping` and the health snapshot against a loopback peer that accepts and then goes silent, and asserts the peer observes EOF — the leaked worker used to hold the socket open indefinitely. Runs in CI, no fixture needed.
- `./scripts/e2e-docker.sh` end to end against the containerized OpenSSH server (key auth, non-root user): 8 checks pass, including the exec runtime suite wired in here.
- `cargo clippy` for the lib under both feature sets, and the two `Rust clippy` invocations as CI runs them, now report nothing.
- Containerised OpenSSH 9.7 fixture (`scripts/sshd-fixture`) with the opt-in `exec_fixture` suite: 4 tests covering the stderr flood, the timeout deadline, the timeout audit entry, and a health snapshot collected over the shared capture path.
- An 8 MiB stderr flood used to stall past a 30 s deadline; it now drains in under 2 s with both streams captured and truncated to `max_output_bytes`.
- Measured limitation (not a regression): a pty-less exec channel's remote command survives the client closing the channel or dropping the connection (`docker exec ps` shows the process, and its `/tmp` marker appears after the client returned). Only the pty path reaps the process group, so a guaranteed stop requires a remote-side `timeout(1)`.
- Test suites, each run twice on the merge candidate: `cargo test --lib` (default / tauri feature) 609 pass, and `cargo test --no-default-features --lib` 601 pass — the difference is the `tauri`-gated unit tests. Both counts sit two above the previous run, which is the pair of `Host` tests added here showing up in each configuration. `cli_contract` 29, `cli_smoke` 33, `daemon_integration` 57 and `connect_deadline` 2 all pass with `--no-default-features`. The `exec_fixture` suite still requires the containerised sshd and is unchanged here.
- `cargo clippy -- -D warnings` is clean on all four targets CI uses: the CLI and MCP binaries together, the daemon binary, the library under default features, and the library with `--no-default-features`. `cargo fmt --check` is clean.
- Risk classification for the four new SFTP operations was measured against the shell equivalents rather than assumed: `risk` on `sftp rm-rf /` returns `blocked`, `sftp rm-rf <path>` returns `high`, `sftp rename <a> -> <b>` returns `medium`, and `sftp rm` / `sftp rmdir` return `low`. The CLI, MCP and desktop paths additionally refuse `high` and `blocked` at the authorisation step before any connection is opened, and neither the MCP schema nor the daemon body schema has a `force` field, so those surfaces cannot override the verdict at all.
- Every deletion was checked in both directions: `git grep` over the Rust sources and over `src/` confirms `emit_bus`, `is_headless`, `is_cli` and `policy_risk_rules` have no remaining reference (the frontend never named them), while `policy.rs` keeps its `RiskRules` import because the `AgentPolicyFile` field still uses it, and `Host::emit` stays because its tests do. The four `TODO(unwired)` notes were removed with the code they annotated, so a grep for that string now returns only notes that are still true — the property that made the note worth deleting.
- Frontend: `npm test` 79 pass across 13 files (including new `AlgoPrefsDialog` and `HostList` cases), `npm run lint` clean over 96 files, `npm run build` succeeds, and `node scripts/check-i18n.mjs` reports no missing keys.
- `src-tauri/gen/schemas/macOS-schema.json` is reverted rather than committed: a local build re-emits the same ACL schema with its object keys in a different order (44 insertions against 44 deletions, no content change), while `desktop-schema.json` regenerated byte-identical in the same run. Committing that churn would add an unreviewable no-op diff.
- The wire-up was re-checked against the built binaries rather than against the diff, because "unreachable" is exactly the failure mode that a diff cannot show. `agent2ssh sftp --help` lists `rename`, `rm`, `rmdir` and `rm-rf`; `agent2ssh known-hosts --help` lists `forget` beside `import`; and `agent2ssh risk` returns the mirrored verdicts end to end — `sftp rm-rf /` → blocked, `sftp rm-rf /home` → high, `sftp rename <a> -> <b>` → medium, `sftp rm` / `sftp rmdir` → low — matching `rm -rf /`, `rm -rf /home` and `mv` measured through the same command. `mcp_stdio_end_to_end_initialize_tools_and_risk` performs the real stdio handshake and asserts `tools/list` returns 58. Every symbol the removed `TODO(unwired)` notes named now has a caller outside its own module, established by `git grep` rather than assumed; the four that remain uncalled are listed in `docs/reports/redundancy-audit.md` as deliberate.
- `known-hosts forget` was exercised against a scratch `HOME` holding a four-line `known_hosts`. The bare-hostname entry was removed, and a second run with `--port 2222` removed the `[host]:port` entry; a multi-alias line (`other.host,10.0.0.5`) and a hashed `|1|...` line were left intact; the leading comment survived; a `.bak` was written; and a host that was not present printed a notice and exited 0. That `.bak` is the recovery path, which is why the command needs no approval prompt — the same reasoning the import half already relied on.
- Every surface was exercised against the built binaries rather than against the diff, because "wired up" is the claim that a diff is worst at showing. **CLI**, in an isolated `AGENT2SSH_CONFIG_DIR`: `redact list` seeds 10 built-in rules and writes `redact_rules.json` at `0600`; `redact add` marks the new rule `is_builtin: false`; `redact update` changes only the replacement; `redact remove` without `--force` exits **2** and names the built-in status of the rule; `redact reset` restores exactly the seeded set; `highlight add --literal 'a.b'` stores `a\.b` and `highlight update --literal` without `--new-keyword` exits 1 with the reason; `algo set --kex` overrides only key exchange and writes `ssh_algos.json`; `algo clear` removes the file; `--daemon localhost` is treated as local and an unknown alias fails loudly instead of silently reading the local file. **MCP** over a real stdio handshake: `tools/list` returns **71** entries with the 13 new names at positions 58–70 and nothing renumbered below them; `ssh_redact_rule_remove` with no `force` key is rejected by the schema and with `"force": false` by the handler, which is the case `required` cannot catch; `ssh_highlight_rule_add` with `literal: true` stores `a\.b`; `ssh_algo_prefs_set` with no list named is refused. **Daemon** over HTTP: `GET /redact/rules` without a token is **401**, and each of the 13 routes was driven with the token — add/update/delete with `force` missing and `force: false` both answering **400** and `force: true` answering 200, `reset` restoring the built-in set, the literal escape surviving as `a\.b`, and `PUT /algo/prefs` holding whole-table semantics (a partial body is 422, and the read-merge-write the CLI performs leaves the other seven lists untouched). The three rule files land at `0600`.
- Exit codes are part of the CLI contract here, so the two failure modes are kept apart: a removal that was not confirmed exits **2** and a rule that does not exist exits **1**, which is what lets a script tell "you forgot `--force`" from "that rule is not there". Both are asserted, together with the fact that a refused removal leaves the rule in place.
- Test suites on the merge candidate: `cargo test --lib` (default/tauri feature) **618** pass, `cargo test --no-default-features --lib` **610** pass, and with `--no-default-features` `daemon_integration` **58**, `cli_smoke` **33**, `cli_contract` **30**, `connect_deadline` **2** and `exec_fixture` **4** all pass. `daemon_integration` and `cli_contract` are each one above the counts recorded for the previous change — the route-parity test and the new lifecycle contract test — and `cli_smoke` moves 58 → **71** in its `tools/list` assertion.
- `cargo clippy -- -D warnings` is clean on all four targets CI uses: the CLI and MCP binaries together, the daemon binary, the library under default features, and the library with `--no-default-features`. `cargo fmt --check` is clean after running `cargo fmt`, which touched only the two binaries this change edits.
- Frontend is untouched by this change and re-verified anyway: `npm run lint` clean over **101** files, `npm test` **121** pass across 17 files, `npm run build` succeeds, and `node scripts/check-i18n.mjs` reports no missing keys.
- `docs/api.yaml` was checked against the daemon's router rather than against the previous revision of itself: 84 `.route(..)` calls, minus the two console paths, equal the 82 entries under `paths:`, with nothing on either side unmatched. The new parity test was then shown to be sensitive rather than assumed to be — deleting the `/algo/prefs` entry from the spec makes it fail and name that path, and restoring the file makes it pass.
- The three contract descriptions corrected above were each contradicted by the running binary before they were rewritten: `redact add '('` exits non-zero, `redact add <same pattern twice>` fails with `a rule for this pattern already exists` (including when the pattern duplicates a built-in, which lives in the same file), `redact add PATTERN ''` is accepted, `highlight add '('` is accepted, and `highlight add` with `--color red` fails with the hex-colour message. Both halves of the redact/highlight asymmetry are now asserted, so a Rust-side regex check added to `highlight` later would fail the suite rather than pass it silently.

## [0.3.0] - 2026-08-12

### Added
- **Safe terminal broadcast**: Added token-owned live terminal IDs and preview/run endpoints for explicit multi-terminal command broadcasts. Every target passes scope, gate, rate-limit, effective-risk and approval checks before any input is enqueued, with per-target audit/results and an explicit desktop “Broadcast and run” action.
- **Conflict-aware portable config sync**: Added stable SHA-256 configuration digests, local/remote/diverged status summaries across CLI, daemon, and desktop, plus a reusable sync transport abstraction with a deterministic fake backend for tests.
- **Structured terminal workbench**: Added marker-backed command blocks with color rails, search, navigation, safe plain-text copy, and structured metadata for future audit consumers.
- **Session recordings**: Added opt-in asciicast v2 terminal recording, protected local storage, daemon and desktop management APIs, variable-speed playback, and confirmed audited deletion.
- **CLI completions**: Added Bash, Zsh, Fish, and PowerShell completion generation with read-only dynamic candidates for configured and active resources.
- **Terminal highlight UX**: Wired persisted highlight rules into xterm decorations and added desktop controls for adding, enabling, deleting, and resetting rules.
- **Desktop diagnostics/setup surfaces**: Added in-app structured system reports, CLI PATH install/remove controls, and jump-host selection for port forwards.
- **Secrets vault**: Encrypted backup/sync of `secrets.enc` with Argon2id and AES-256-GCM, lifecycle registry for daemon/managed processes, and structured error codes across the Rust backend.
- **Redaction and sanitization pipeline**: Centralized output redaction (`redaction.rs`, `sanitize.rs`, `copy_redact.rs`) applied consistently across exec, audit, export, and terminal surfaces; secrets never leak into logs or audit exports.
- **Embedded SSH forwarding**: Full local/remote port-forward support over the in-process SSH transport (`forward.rs`), replacing remaining system `ssh` runtime dependencies.
- **Jump-host chain**: Bastion/proxy chain support (`jump_chain.rs`) for multi-hop connections over embedded `direct-tcpip`.
- **Prompt waiter**: Interactive prompt detection and waiting (`prompt_waiter.rs`) for password/OTP/confirmation prompts inside PTY sessions.
- **Container discovery**: Container/platform discovery module (`container_discovery.rs`) for SSH targets inside containers.
- **SSH algorithm management**: Explicit SSH algorithm/curve control (`ssh_algo.rs`), URL safety validation (`url_safety.rs`), path resolution guardrails (`path_resolver.rs`), WebSocket drain handling (`ws_drain.rs`), and OSC/IPC bridging (`osc_ipc.rs`).
- **Snippets**: Reusable command snippet storage with validated CRUD across desktop, CLI, daemon HTTP, and MCP, plus WebDAV synchronization (`snippets.rs`).
- **Encrypted backup crypto**: Key derivation and backup encryption primitives (`backup_crypto.rs`) used by the encrypted sync path.
- **CI lint checks**: Added Biome config (`biome.json`), frontend test infrastructure (Vitest + Testing Library), and CI lint gates.
- **Branch protection**: Documented branch protection policy (`docs/guides/branch-protection.md`) and added `.github/CODEOWNERS`.

### Changed
- **Versioned WebDAV commits**: WebDAV schema v2 uploads immutable per-sync objects and commits markers with ETag compare-and-swap; push/pull reject divergent overwrites unless `--force`, pull validates a complete snapshot before replacement, and schema v1 pulls preserve newer local-only snippets.
- **WebDAV sync trust boundary**: `known_hosts.json` is no longer part of WebDAV sync payloads. Pulls from older remote manifests tolerate and skip legacy `known_hosts.json` entries so local SSH host-key trust state is not overwritten across machines.
- **Desktop internationalization coverage**: Completed Chinese translations for the current desktop surface, including SSH fingerprint confirmation, connection progress, WebDAV Sync, MCP binding removal, the Sync module label, and the React error recovery screen.
- **CI release runners**: Use Intel macOS runners for x86 builds; release workflow portability and macOS runner environment fixes; stop caching Rust toolchain binaries.
- **Windows Tauri command import**: Fixed the Windows Tauri command import in `tauri_commands.rs`.

### Fixed
- **Recent feature completion**: Forward creation now waits for authenticated listener readiness, multi-rule batches roll back on partial failure, SSH `Include` supports multi-directory globs, Fish PATH entries use native syntax, and `v`-prefixed release tags compare correctly.
- **Private-key passphrases**: Store passphrases in the encrypted secrets vault, preserve them during edits, migrate them during host renames, clear cache entries on lock/removal/auth failure, and refuse plaintext persistence while locked.
- **Regression hardening**: Redacted host credentials from every transport response, exposed real tunnel state/traffic in the desktop, aligned highlight validation with JavaScript regex semantics, supported alternate-screen highlighting, rejected malformed remote versions, and removed the browser-opening side effect from URL tests.
- **Tauri bundle build**: Added the missing single-instance plugin dependency for default desktop builds and restored the fingerprint confirmation commands used by the desktop connection flow.
- **Icon packaging**: Regenerated the v3 32px app icons as RGBA PNGs so Tauri bundle generation accepts the icon set.

### Verified
- `cargo test --manifest-path src-tauri/Cargo.toml --no-default-features --lib`
- `npm run build`
- `git diff --check`
- `npm run tauri:build`
- Desktop i18n static audit: 442 checked keys, 0 missing translations, 0 placeholder mismatches

## [0.2.1] - 2026-06-21

### Added
- **App-managed credential encryption**: Added a master-password credential store backed by Argon2id and AES-256-GCM in `~/.agent2ssh/secrets.enc`. `hosts.json` now keeps only `$agent2ssh-secret$` references after migration.
- **Credential unlock surfaces**: Added desktop unlock/change-password commands and CLI `secrets status` / `secrets set-password`, with `AGENT2SSH_MASTER_PASSWORD` support for headless CLI/MCP/daemon use.
- **WebDAV config sync**: Added `agent2ssh webdav push|pull|status` for syncing portable config plus encrypted `secrets.enc`, with local pre-sync backups and a global `sync_version.json` marker on every sync.

### Changed
- **Credential storage model**: SSH host and proxy passwords no longer rely on system key management. Secrets are managed by Agent2SSH encryption and remain unavailable while the store is locked.
- **Build defaults**: Local Tauri bundles no longer create updater signing artifacts by default, so builds do not require `TAURI_SIGNING_PRIVATE_KEY` unless release signing is explicitly enabled.

### Added
- **Control-plane safety layer**: Added daemon-level execution gate controls, execution rate/session limits, unified policy-as-code validation, and audit-window anomaly detection.
- **Execution gate**: Added `agent2ssh pause/resume/status`, daemon 423 rejection for paused non-desktop sources, gate audit entries, and `gate_rejected` / `gate_changed` events.
- **Execution limits**: Added `execution_limits.toml` with per-source, per-host, and per-tag rate/session limits, plus 429 rejection auditing and `limit_rejected` events.
- **Unified policy files**: Added `policy.toml` / `policy.json` support for colocating risk rules and approval policies, with `agent2ssh policy validate` and `agent2ssh policy test`.
- **Anomaly detection**: Added `anomaly.toml`, source burst detection, sensitive command pattern detection, after-hours high-risk detection, `anomaly_detected` events, webhook support, and Live Activity anomaly highlighting.
- **Embedded jump-host and forwarding transport**: Added embedded `direct-tcpip` bastion proxy channels plus local/remote forwarding over the in-process SSH transport, removing the remaining system `ssh`/`scp`/`sshpass` runtime dependency from exec, SFTP, terminal, session, connection, health, and forward paths.
- **Terminal limit coverage**: WebSocket `/terminal` now participates in daemon session concurrency limits and applies execution rate limits to completed terminal input lines.
- **Embedded SSH key generation**: Local Ed25519 key generation now uses the Rust backend and the operating system CSPRNG instead of shelling out to `ssh-keygen`.
- **Portable daemon lifecycle control**: Daemon status/stop/restart and health checks now use Rust process and HTTP APIs instead of shelling out to `kill`, `taskkill`, `tasklist`, or `curl`.

### Changed
- **Approval scoping**: Multi-host execution and playbook approvals now apply only to the approved host or step. Explicit `force` still applies to the whole requested operation when policy permits.
- **Mutation authorization and audit semantics**: Non-exec mutation paths now use the normal high-risk approval/force semantics instead of implicitly forcing authorization. PTY session writes use line-buffered authorization for completed shell input, and session/forward/connection operations write operation-level audit entries.
- **Approval context risk details**: Daemon approval context now carries the effective risk produced by the authorization path, including trusted host overrides and matched approval policy names when available.
- **Desktop risk previews**: Desktop exec and session input previews now include host-level `risk_override` before prompting.
- **Remote daemon tag scope**: Client-side `remotes.toml` tag checks now read host tags from the remote daemon before forwarding, so tag-based remote scope decisions use the remote daemon as the source of truth.
- **Team config import semantics**: `config-import` now updates changed same-name hosts while preserving local key/password material, matching the existing import preview.
- **Policy compatibility**: Runtime policy loading now prefers unified policy files and falls back to legacy `risk_rules.toml` / `approval_policies.toml` when no unified policy exists.
- **Connection management**: Connection status/connect/disconnect now retain embedded SSH sessions instead of creating ControlMaster sockets; `socket_path` remains `null`.
- **Documentation**: README, architecture, OpenAPI, configuration, daemon quickstart, MCP quickstart, and plan docs now describe the completed G-stage control-plane capabilities and the exact PTY/session audit boundary.

### Fixed
- **Desktop mutation parity**: Desktop-local SFTP, session, forward, and connection operations now use the same high-risk approval/force semantics as daemon, CLI, and MCP paths.
- **Desktop operation audit**: Desktop-local session, forward, and connection mutations now append operation-level audit entries for success and failure.
- **Vite production chunks**: Production frontend builds now split terminal, React, UI, icon, and runtime vendor chunks without the previous large-chunk warning.
- **Terminal session-limit race**: WebSocket `/terminal` and REST session open now reserve session-limit capacity atomically before opening the backend PTY, so concurrent opens cannot bypass `max_sessions`.

## [0.1.1] - 2026-06-16

### Added
- **Live Agent Activity**: Added a desktop activity panel that subscribes to daemon SSE events and polls recent audit entries, giving local visibility into SSH exec/session activity initiated by agents, CLI, daemon API, or the desktop app.
- **Daemon-backed MCP sessions**: MCP PTY sessions now route through the local daemon session registry by default when the daemon is reachable, while retaining process-local fallback when it is not.
- **Desktop session takeover**: The desktop Session panel can list daemon-managed sessions, attach to sessions created by MCP/CLI/daemon clients, read output, write input, close sessions, and fall back to local sessions when daemon access is unavailable.
- **Session takeover safety controls**: Session takeover now supports automatic tailing, read-only attach, active-session read-only mode, and high-risk PTY input confirmation before writing to the session.
- **S6 regression report**: Added `docs/s6-regression-report.md`, covering real-server validation for MCP daemon routing, source attribution, SSE session events, preview redaction, and cleanup proof.
- **Audit context tests (S1)**: 6 new tests covering `exec-multi` and `playbook` audit context propagation — verifying that `reason` and `change_id` survive the full write → JSONL → read round-trip for multi-host and multi-step scenarios.

### Changed
- **Daemon event stream**: Session open/write/read/close and WebSocket exec stream output now publish structured local events with source, host/session identifiers, command metadata, and bounded input/output previews.
- **Source attribution**: CLI, MCP, daemon, and desktop paths now carry standard `source` attribution into daemon events and audit entries, with `AGENT2SSH_SOURCE` available for agent-specific labels such as `codex`, `claude-code`, or `opencode`.
- **Live Activity filtering and alerts**: The activity panel now supports source/type/text filtering, expandable event details, sensitive preview redaction, and visible alerts for high-risk non-desktop activity.
- **Documentation**: Updated architecture and plan docs to reflect the completed S5-S8 activity visibility and desktop takeover work.
- **Test cleanup (S1-3)**: Eliminated all `unused variable` and `dead_code` compiler warnings in `cargo test --no-default-features` and `cargo test --no-default-features --features daemon` builds.
- **Real environment regression (S2)**: Full CLI, daemon HTTP, and MCP regression against a live SSH server — verified host management, exec/exec-multi (with reason/change_id), playbook run, audit (table/jsonl/csv), audit export, health-snapshot, doctor; confirmed MCP tool count at 50. No high-priority issues found. Report: `docs/s2-regression-report.md`.
- **Documentation & contract consistency (S3)**: 9 new tests ensuring README, `docs/skills.md`, `docs/api.yaml`, MCP schema, and daemon handlers stay in sync — MCP tool-name cross-check against `skills.md` (S3-1), request/response schema fixture tests for `/exec`, `/exec-multi`, `/playbooks/run`, `/audit/export` (S3-2), CLI `--help` alignment for `exec`, `exec-multi`, `playbook run` (S3-4). README MCP tools table deduplicated to a summary with link to `docs/skills.md` (S3-3).
- **Release quality gate (S4)**: Established fixed pre-release acceptance commands (`npm run build`, `cargo check` for all binary targets, two `cargo test` configurations). Tauri bundle build verified — `Agent2SSH.app` and `.dmg` generated with correct `agent2ssh-app` main binary. Installation scripts (`verify-install.sh`, `prepare-sidecars.sh`, `generate-checksums.sh`) validated and `verify-install.sh` fixed to avoid hanging on daemon/MCP `--help`. Created `docs/release-checklist.md` as a repeatable pre-release procedure.

### Fixed
- **Audit chain (F4-4)**: `exec-multi` and `playbook run` now correctly propagate `reason` and `change_id` through to every per-host audit entry. Previously, audit entries created via multi-host execution or playbook steps could lose the operation context.
- **MCP tool count**: Corrected documented MCP tool count from 31 to 50, reflecting all tools added in F2–F6 phases (host health, audit export, playbook run, metrics trends, etc.).
- **OpenAPI `/exec-multi` response**: Fixed response schema for the `/exec-multi` daemon endpoint to include `reason` and `change_id` fields in the request body, matching the actual implementation.

### Verified
- `npm run build`
- `git diff --check`
- Real-server S6 regression against `107.174.36.91`
- Browser render checks for the Live Activity and SessionPanel UI changes

## [0.1.0] - 2025-06-12

### Added
- **Interfaces**: Tauri desktop app, CLI, MCP stdio server (50 tools), HTTP/WebSocket daemon, Web Console
- **Host Management**: CRUD, SSH config import, ProxyJump/bastion, tags, per-host risk override, SSH key association
- **Command Execution**: Single-host exec, multi-host exec (by name or tag), ping, ControlMaster connection pooling
- **Safety**: 4-tier risk classification (low/medium/high/blocked), configurable risk rules, approval queue with TTL, audit log, desktop approval dialog
- **File Transfer**: SFTP upload/download/ls/stat/mkdir
- **Sessions & Tunnels**: Interactive PTY sessions, local/remote port forwarding
- **Automation**: Webhook notifications (HMAC-SHA256 signing, Slack Block Kit), Playbooks (command sequences), Remote daemon support
- **SSH Keys**: Ed25519 generation, import, delete, key dropdown in host form
- **Security**: Daemon token 0600 on Unix, SSH key permission enforcement, WebSocket exec stream auth, webhook outbound protection
- **CI/CD**: 4-platform build matrix, Tauri bundle job, Homebrew formula
- **Testing**: 137 unit tests + 56 integration tests + 24 CLI smoke tests

### Security
- Daemon token file restricted to 0600 on Unix systems
- SSH private key permissions enforced to 0600 after generation/import
- WebSocket exec stream requires Bearer token authentication
- Webhook outbound uses non-blocking fire with configurable timeout and HMAC-SHA256 signing
- Approval requests have configurable TTL (default 300s), expired requests auto-marked as timed_out
