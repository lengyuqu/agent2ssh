# Agent2SSH 界面设计定稿 v3：午夜指挥舱（Midnight Ops）

> 作者：UI 设计师（像素君） · 2026-08-28
> 定稿过程：v1 终端布局 → v2 审查台定位重构 → 用户判定视觉平淡、布局陈旧，推倒重来 → 三方向竞稿（A 午夜指挥舱 / B 暗房警报 / C 纸面裁决）→ **用户选定 A**。
> 本文档为最终设计规格，v1/v2 布局与配色结论全部作废，仅产品定位（配置管理 + AI 命令审查）延续。

---

## 1. 设计语言一句话

**深蓝黑分层画布上，风险色讲故事，电光青只给行动。**
开屏即一个大数字：「3 项待您裁决」。等宽字体承载一切数据（命令、主机、时间、指标），界面气质是任务指挥中心，不是后台管理系统。

## 2. 核心设计令牌（新体系，替代旧青绿主题）

产品默认仅一套深色皮肤（指挥舱不做多主题——工具气质优先，主题系统后续有需求再加）：

```css
:root {
  /* 画布分层（由深到浅四级，层级感全靠它，不用阴影） */
  --canvas-0: #0A0F18;   /* 最深：侧边栏 / 命令块底 */
  --canvas-1: #0E1420;   /* 主画布 */
  --canvas-2: #131A28;   /* 卡片 / 悬浮层 / 选中行 */
  --canvas-3: #1A2334;   /* 激活浮起（Dialog / Palette） */

  /* 线与描边 */
  --line:        #1C2637;   /* 分隔线 */
  --edge:        #263042;   /* 控件描边 / 输入框 */
  --edge-strong: #33405A;   /* hover 描边 */

  /* 文字 */
  --text-1: #E6EDF3;   /* 主文字 */
  --text-2: #8B98AC;   /* 次要 */
  --text-3: #5A6778;   /* 弱化 / 时间戳 / 注脚 */

  /* 行动色（唯一强调，只给「用户可执行的动作」） */
  --action:        #2DD4BF;
  --action-hover:  #4AE0CD;
  --action-ink:    #062B26;   /* action 上的文字 */

  /* 风险三族（产品主叙事色，全界面同源） */
  --risk-low:      #34D399;
  --risk-medium:   #FFB224;
  --risk-high:     #F25555;
  --risk-blocked:  #F25555;   /* 实底 + canvas-0 文字 */

  /* 成功 / 信息语义复用风险族：已批准=low 绿，已拦截=high 红，自动放行=text-3 */

  /* 圆角：控件 9px / 卡片 10px / 窗口 14px
     字体：Inter（UI）+ JetBrains Mono（一切数据）
     字号：10.5 chips / 11 注脚 / 12 正文小 / 12.5-13 正文 / 14.5 命令主角 / 26-34 开屏数字 */
}
```

**三条铁律**：
1. 电光青 `--action` 全界面只出现在可点击的行动（按钮、当前导航、链接）上，绝不用于装饰；
2. 绿/琥珀/红风险三族是唯一的状态色，badge、队列左缘、时间线节点、审计筛选、Toast 全部同源；
3. 层级靠画布四级分层 + 1px 描边，禁用阴影和渐变。

## 3. 界面清单（方向 A 语言贯穿）

### 3.1 审批工作台（默认主界面，已出视觉稿）
- 顶栏：A2 标识 + Agent 在线 pills（canvas-0 底）。
- 左栏（150px，canvas-0）：审批队列（青色激活态 + 数字 badge）/ 主机配置 / 审计日志 / MCP Agents / 策略规则 / ⌘K。
- 主区开屏：30px 大数字「3」+「项待您裁决」+ 弱化统计（执行中 / 今日已审）。
- 队列（250px）：每卡 = Agent→主机 + 风险 chip（HIGH/MED/LOW 等宽大写）+ mono 命令；选中卡 canvas-2 + 左缘 2px 风险色，未选中项压暗（text-2）。
- 详情栏：命令块（canvas-0 底、13px mono、高危片段 risk-high 着色）+ 命中规则 + 时间线 + 三按钮（拒绝 / 编辑后执行 / 批准执行=青色实底 action-ink 字）。
- 键盘：↑↓ 选卡、↵ 聚焦详情、⌘↵ 批准（高危必须两步确认）、⌫ 拒绝。

### 3.2 主机配置中心（已出视觉稿）
- 主机卡片：状态点 + 名称 + 「AI 可用 / 仅人工」chip + 延迟（mono）+ 跳板链 / 密钥指纹 / 本月执行数。
- **策略摘要行**是本页灵魂：`low 自动放行 · med/high 需审批 · 删除类禁止`，三段分别着风险三族色。
- 离线/仅人工主机整卡 opacity 0.75 弱化。

### 3.3 审计日志（已出视觉稿）
- 顶部风险计数 chips（当前筛选项实底反白，其余描边）。
- 列表行：mono 时间戳（text-3）+ mono 命令（按风险族着色）+ 右侧结果（已批准=绿 / 已拦截=红 / 自动放行=text-3）。
- 筛选：Agent / 主机 / 风险级；支持导出与命令回放（回放唤起下沉终端）。

### 3.4 终端（下沉视图）
- 不设独立主界面；从审批「批准执行」与审计「回放」唤起，右侧抽屉（canvas-0 底）。
- 现有 xterm 交互与 `command-block-bar` 保留，配色对齐本令牌（命令块侧条改用风险三族）。

### 3.5 ⌘K 命令面板
- canvas-3 浮层、宽 460px；分组：待审批（置顶）→ 跳转 → 快捷操作 → MCP 工具（`>` 前缀）→ 连接主机（末组）。

### 3.6 Toast
- canvas-3 底 + 左缘 3px 语义色（复用风险三族 + 青色=后台任务进行中）；右上堆叠 ≤3 条，3.5s 消退，错误常驻。

## 4. 与现有代码的迁移策略

| 现状 | 处理 |
|------|------|
| 5 套主题（light/dark/dracula/nord/solarized） | 主题选择器保留，但新增「指挥舱」为默认主题（实现为替换 light/dark 的默认值，即 `:root` 默认 token 换血）；其余主题不动，兼容存量用户 |
| RiskBadge 四级映射 | 保留结构，色值对齐 `--risk-*` |
| shadcn 组件层 | 保留 API，重写 `ui/*.tsx` 内的视觉映射到新 token |
| `.terminal-surface` / command-block | 保留交互，配色对齐 |
| i18n | 新文案全部入 `src/i18n.tsx`（CI 门禁 0 缺失） |

## 5. 实施顺序

1. **Token 换血**：`index.css` 写入指挥舱默认主题 + 保留旧主题分支（半天）
2. **审批工作台**：主布局改造 + 队列/详情/操作三栏（核心工作量）
3. **主机配置中心 + 审计日志**皮肤对齐
4. **Footbar → 状态顶栏**（并入 Agent pills 行，取消独立 footbar）
5. **⌘K / Toast / 终端抽屉**收尾
每步 `npm test && npm run build` + `npm run tauri:dev` 目检；完成后巡检 docs/ 漂移并同步 `docs/skills.md`（如有 UI 相关 MCP 工具描述变更）。

## 6. 可访问性底线

- text-1/2 对 canvas-1 对比度 ≥7:1，text-3 ≥4.5:1（注脚级允许 4.5）
- risk 三族在 canvas-1/2 上 ≥3:1（大字/chip 级），badge 均带文字不只靠色
- 高危命令批准永不绑定单键；焦点环用 `--action` 2px；`prefers-reduced-motion` 全遵守
