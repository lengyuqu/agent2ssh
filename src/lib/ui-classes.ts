/** Shared Tailwind class strings reused across panels. */

/** Standard form-field label wrapper (grid + small caption typography). */
export const labelCls = "grid gap-1.5 text-sm font-medium text-foreground/90";