# 2026-06-18

## 推送：pushscript 执行 GitHub 同步
- 解读了 `pushscript/push-github.sh` 脚本：创建临时分支 → git rm --cached 剥离私有目录（.workbuddy/ 等 AI 工具目录）→ force-push 到 github/main
- 执行成功：剥离 `.workbuddy/` 后推送至 `https://github.com/lengyuqu/agent2ssh.git`
- pushscript/ 本身也为剥离目标之一（未跟踪跳过）
- git233 远程不受影响
